# Simulator service

Publishes simulated robot telemetry to MQTT.

See [`../../docs/feature-spec.md`](../../docs/feature-spec.md) (F1) for the spec.

Implementation lands in `main.py` in the next commit.
