# Ingest service

FastAPI service that subscribes to MQTT and writes validated telemetry into PostgreSQL.

See [`../../docs/feature-spec.md`](../../docs/feature-spec.md) (F2) for the spec.

Implementation lands in `main.py` in a follow-up commit.
